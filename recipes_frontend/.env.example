# Feature flag: use local mock data (default true)
REACT_APP_USE_MOCK=true

# Optional: base URL for real backend API (e.g., https://api.example.com)
# When REACT_APP_USE_MOCK=false, this will be used for requests.
REACT_APP_API_BASE_URL=
